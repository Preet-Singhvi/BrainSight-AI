import { createAction } from '@reduxjs/toolkit'

export const SetPatientDialog = createAction('SetPatientDialog')
export const SetFilesDialog = createAction('SetFilesDialog')
export const SetViewFiles = createAction('SetViewFiles')